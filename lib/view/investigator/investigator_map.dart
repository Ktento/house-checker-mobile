import 'package:flutter/material.dart';

class InvestigatorMap extends StatelessWidget {
  const InvestigatorMap({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: const Center(
      child: Text("マップ"),
    ));
  }
}
